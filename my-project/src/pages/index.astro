---
import Layout from '../layouts/Layout.astro';

const listings = [
  {
    id: 1,
    image: 'https://cdn6.ep.dynamics.net/s3/rw-propertyimages/025d-H3473905-192570098__1773729572-112408-SatinwoodCt11-1.jpg',
    suburb: 'Bardon',
    address: '11 Satinwood Court',
    street: 'Bardon QLD 4065',
    price: 'Best Offers by 16 April',
    beds: 5, baths: 6, cars: 4,
    tag: 'For Sale',
    size: 'large',
    href: 'https://raywhitebulimba.com.au/properties/residential-for-sale/qld/bardon-4065/house/3473905',
  },
  {
    id: 2,
    image: 'https://cdn6.ep.dynamics.net/s3/rw-propertyimages/8929-H3473633-192806009__1773800859-70111-0.png',
    suburb: 'Seven Hills',
    address: '20A Marshall Avenue',
    street: 'Seven Hills QLD 4170',
    price: 'Price By Negotiation',
    beds: 4, baths: 3, cars: 2,
    tag: 'For Sale',
    size: 'tall',
    href: 'https://raywhitebulimba.com.au/properties/residential-for-sale/qld/seven-hills-4170/house/3473633',
  },
  {
    id: 3,
    image: 'https://cdn6.ep.dynamics.net/s3/rw-propertyimages/1a0c-H3474693-192972192__1773876548-26815-Photo-2-Web.jpg',
    suburb: 'Norman Park',
    address: '154 Morehead Avenue',
    street: 'Norman Park QLD 4170',
    price: 'Best Offers by 22 April',
    beds: 3, baths: 1, cars: 2,
    tag: 'For Sale',
    size: 'square',
    href: 'https://raywhitebulimba.com.au/properties/residential-for-sale/qld/norman-park-4170/house/3474693',
  },
  {
    id: 4,
    image: 'https://cdn6.ep.dynamics.net/s3/rw-propertyimages/772d-H3473513-192064272__1773457507-13356-28VincentStreetAuchenflowerInternet01.jpg',
    suburb: 'Auchenflower',
    address: '28 Vincent Street',
    street: 'Auchenflower QLD 4066',
    price: 'Pre-Market Opportunity',
    beds: 3, baths: 2, cars: 2,
    tag: 'Pre-Market',
    size: 'square',
    href: 'https://raywhitebulimba.com.au/properties/residential-for-sale/qld/auchenflower-4066/house/3473513',
  },
];

const prestige = [
  {
    image: 'https://cdn6.ep.dynamics.net/s3/rw-propertyimages/b485-H3473001-192442478__1773707732-39332-6-print-70-longland-st-203.jpg',
    suburb: 'Newstead',
    address: '203/70 Longland Street',
    price: 'Auction 18 April — The Calile Hotel',
    beds: 2, baths: 2, cars: 1,
    desc: 'Engineered timber floors, Miele appliances and sweeping views over the historic Gasometer — a rare inner-city apartment of genuine architectural distinction.',
    tag: 'Forthcoming Auction',
    href: 'https://raywhitebulimba.com.au/properties/residential-for-sale/qld/newstead-4006/apartment/3473001',
  },
  {
    image: 'https://cdn6.ep.dynamics.net/s3/rw-propertyimages/e543-H3474534-192843678__1773807208-74050-IMG2700p.jpg',
    suburb: 'Bridgeman Downs',
    address: '5 Neville Road',
    price: 'For Sale by Negotiation',
    beds: 3, baths: 2, cars: 6,
    desc: 'An elevated 607m² hilltop allotment surrounded by multi-million-dollar acreage homes — a rare opportunity to landbank or rebuild in one of Brisbane\'s most prestigious pockets.',
    tag: 'Blue-Chip Land',
    href: 'https://raywhitebulimba.com.au/properties/residential-for-sale/qld/bridgeman-downs-4035/house/3474534',
  },
];

const stats = [
  { value: 1000, suffix: '+', prefix: '',  label: 'Ray White Offices' },
  { value: 500,  suffix: 'K+',prefix: '',  label: 'Buyers in Network' },
  { value: 70,   suffix: 'B+',prefix: '$', label: 'Annual Sales' },
  { value: 1,    suffix: '',  prefix: '#', label: 'Network in Australia' },
];

const reasons = [
  { n: '01', title: 'Deep Local Knowledge', body: 'Born and raised in Brisbane, Daniel brings a decade of property economics expertise and hyper-local intelligence across the inner east and south — delivering insights no broad-market agency can match.' },
  { n: '02', title: 'Data-Driven Campaigns', body: 'Every campaign is grounded in data, detail and clear communication. From pricing strategy to buyer targeting, decisions are backed by market evidence — not gut feel.' },
  { n: '03', title: 'Access to 500,000+ Buyers', body: 'Through the Ray White Collective network, your property is exposed to Australia\'s largest active buyer database, ensuring maximum competition and the strongest possible result.' },
];

const toolPhaseColors: Record<string, string> = { Seller: '#ffdd79', Buyer: '#7ec8e3', Investor: '#a8e6a3', Market: '#c8c6c8' };

const showcaseTools = [
  { href: '/tools/selling-costs',    phase: 'Seller',   label: 'Selling Cost Calculator', desc: 'See your true net proceeds after all selling costs.' },
  { href: '/tools/stamp-duty',       phase: 'Buyer',    label: 'Stamp Duty Calculator',   desc: 'QLD stamp duty, FHB concessions & upfront costs.' },
  { href: '/tools/mortgage',         phase: 'Buyer',    label: 'Mortgage Calculator',     desc: 'Weekly or monthly repayments + amortisation chart.' },
  { href: '/tools/investment-yield', phase: 'Investor', label: 'Investment Yield',        desc: 'Net yield, cash flow & 10-year capital projection.' },
  { href: '/tools/renovation-roi',   phase: 'Seller',   label: 'Renovation ROI',          desc: 'Which upgrades add the most value before you sell.' },
  { href: '/tools/suburb-match',     phase: 'Buyer',    label: 'Suburb Match Quiz',       desc: 'Answer 8 questions to find your ideal Brisbane suburb.' },
  { href: '/tools/valuation',        phase: 'Seller',   label: 'Instant Valuation',       desc: 'Get an indicative property value in minutes.' },
  { href: '/tools/heatmap',          phase: 'Market',   label: 'Price Heatmap',           desc: 'Visual median price & growth map across Brisbane.' },
];
---

<Layout>

<!-- ════════════════════════════════════════════
     HERO
════════════════════════════════════════════ -->
<section class="relative min-h-dvh flex items-center overflow-hidden" style="background:var(--color-base);">

  <!-- Parallax bg -->
  <div class="absolute inset-0 z-0" id="hero-bg">
    <img
      src="https://images.unsplash.com/photo-1600596542815-ffad4c1539a9?w=1920&q=90"
      alt="Modern white Brisbane property"
      class="w-full h-full object-cover"
      style="filter:grayscale(8%) brightness(0.42);"
      fetchpriority="high"
      width="1920" height="1080"
    />
    <div class="absolute inset-0" style="background:linear-gradient(120deg,rgba(10,8,6,0.78) 0%,rgba(10,8,6,0.25) 100%);"></div>
  </div>

  <!-- Glass card -->
  <div class="relative z-10 w-full max-w-[1400px] mx-auto px-8 lg:px-12 pt-28 pb-20">
    <div class="max-w-2xl" style="background:rgba(0,0,0,0.45);backdrop-filter:blur(18px);-webkit-backdrop-filter:blur(18px);border:1px solid rgba(72,72,72,0.2);padding:3rem 3.5rem;">

      <span class="eyebrow mb-5 reveal" data-delay="0">Daniel Gierach Property · Ray White Bulimba</span>

      <h1 class="reveal" data-delay="80"
        style="font-family:var(--font-serif);font-size:clamp(2.8rem,6vw,5rem);font-weight:400;line-height:1.08;letter-spacing:-0.02em;color:#f0ece4;margin-bottom:1.5rem;">
        Straight Advice.<br />
        <em style="font-style:italic;color:var(--color-gold);">Real Results.</em>
      </h1>

      <p class="reveal" data-delay="160"
        style="font-size:1rem;color:rgba(240,236,228,0.72);line-height:1.8;max-width:42ch;margin-bottom:2.5rem;">
        Brisbane inner east and south property agent at Ray White Bulimba. Local knowledge, honest advice, and a strategy built around your property.
      </p>

      <div class="flex flex-wrap gap-4 reveal" data-delay="240">
        <a href="/listings" class="btn-gold">
          View Collection
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="/appraisal" class="btn-ghost-light">Request an Appraisal</a>
      </div>

      <!-- Stats -->
      <div class="grid grid-cols-2 sm:grid-cols-4 gap-6 mt-10 pt-8 reveal" data-delay="320"
        style="border-top:1px solid rgba(255,255,255,0.12);">
        {stats.map((s) => (
          <div>
            <p class="stat-counter" data-value={s.value} data-suffix={s.suffix} data-prefix={s.prefix}
              style="font-family:var(--font-serif);font-size:1.8rem;font-weight:400;color:#f0ece4;line-height:1;letter-spacing:-0.02em;">
              {s.prefix}{s.value}{s.suffix}
            </p>
            <p style="font-size:0.62rem;font-weight:700;letter-spacing:0.16em;text-transform:uppercase;color:rgba(240,236,228,0.45);margin-top:0.4rem;">
              {s.label}
            </p>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- Scroll nudge -->
  <div class="absolute bottom-8 right-12 hidden lg:flex flex-col items-center gap-2">
    <span style="font-size:0.55rem;font-weight:700;letter-spacing:0.2em;text-transform:uppercase;color:rgba(161,161,161,0.4);writing-mode:vertical-rl;">Scroll</span>
    <div style="width:1px;height:48px;background:linear-gradient(to bottom,rgba(255,221,121,0.4),transparent);"></div>
  </div>
</section>


<!-- ════════════════════════════════════════════
     MARQUEE RIBBON
════════════════════════════════════════════ -->
<div style="background:var(--color-surface-2);border-top:1px solid rgba(72,72,72,0.12);border-bottom:1px solid rgba(72,72,72,0.12);padding:1.1rem 0;overflow:hidden;">
  <div class="marquee-track">
    {[
      'Riverfront Properties',
      'Luxury Penthouses',
      'Heritage Estates',
      'Off-Market Exclusives',
      'Private Collection',
      'Auction Results',
      'Riverfront Properties',
      'Luxury Penthouses',
      'Heritage Estates',
      'Off-Market Exclusives',
      'Private Collection',
      'Auction Results',
    ].map((item, i) => (
      <span class="flex items-center gap-5 flex-shrink-0 px-10" style="font-family:var(--font-sans);font-size:0.62rem;font-weight:700;letter-spacing:0.22em;text-transform:uppercase;color:var(--color-text-muted);">
        {item}
        <span style={`display:inline-block;width:4px;height:4px;background:${i % 2 === 0 ? 'var(--color-gold)' : 'rgba(72,72,72,0.5)'};`}></span>
      </span>
    ))}
  </div>
</div>


<!-- ════════════════════════════════════════════
     SEARCH WIDGET
════════════════════════════════════════════ -->
<section style="background:var(--color-surface-1);">
  <div class="max-w-[1400px] mx-auto px-8 lg:px-12">

    <!-- Tabs -->
    <div class="flex" style="border-bottom:1px solid rgba(28,25,23,0.12);">
      {[
        { id: 'buy',      label: 'Buy' },
        { id: 'sold',     label: 'Sold' },
        { id: 'estimate', label: 'Estimate' },
      ].map((tab, i) => (
        <button class={`s-tab${i === 0 ? ' active' : ''}`} data-tab={tab.id}>{tab.label}</button>
      ))}
    </div>

    <!-- Buy panel -->
    <div id="panel-buy" class="search-panel py-6">
      <div class="flex flex-col lg:flex-row gap-3">
        <div class="flex-1 relative">
          <svg class="absolute left-4 top-1/2 -translate-y-1/2 pointer-events-none" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--color-text-muted)" stroke-width="1.5">
            <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
          </svg>
          <input type="text" placeholder="Search by suburb, street or postcode..." class="obs-input" style="padding-left:2.75rem;" />
        </div>
        <select class="obs-select" style="min-width:150px;">
          <option value="">Any Type</option>
          <option>House</option><option>Apartment</option><option>Townhouse</option><option>Land</option>
        </select>
        <select class="obs-select" style="min-width:130px;">
          <option value="">Any Beds</option>
          <option>1+</option><option>2+</option><option>3+</option><option>4+</option><option>5+</option>
        </select>
        <select class="obs-select" style="min-width:150px;">
          <option value="">Any Price</option>
          <option>Under $2M</option><option>$2M – $5M</option><option>$5M – $10M</option><option>$10M+</option>
        </select>
        <button class="btn-gold" style="white-space:nowrap;flex-shrink:0;">Search Properties</button>
      </div>
    </div>

    <!-- Sold panel -->
    <div id="panel-sold" class="search-panel py-6" style="display:none;">
      <div class="flex flex-col lg:flex-row gap-3">
        <div class="flex-1 relative">
          <svg class="absolute left-4 top-1/2 -translate-y-1/2 pointer-events-none" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--color-text-muted)" stroke-width="1.5">
            <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
          </svg>
          <input type="text" placeholder="Search recently sold by suburb..." class="obs-input" style="padding-left:2.75rem;" />
        </div>
        <select class="obs-select" style="min-width:170px;">
          <option>Last 12 months</option>
          <option>Last 3 months</option><option>Last 6 months</option><option>Last 2 years</option>
        </select>
        <button class="btn-gold" style="white-space:nowrap;flex-shrink:0;">View Sold Results</button>
      </div>
    </div>

    <!-- Estimate panel -->
    <div id="panel-estimate" class="search-panel py-7" style="display:none;">
      <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-6">
        <div>
          <p style="font-family:var(--font-serif);font-size:1.4rem;font-weight:400;font-style:italic;color:var(--color-text-primary);margin-bottom:0.4rem;">
            What is your property worth?
          </p>
          <p style="font-size:0.8rem;color:var(--color-text-muted);line-height:1.6;max-width:44ch;">
            Get an instant digital property report — powered by Ray White, no obligation.
          </p>
        </div>
        <a
          href="https://raywhitecollective.com.au/property-report/my-property-value?da_email_id=DanielGierach"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-gold"
          style="flex-shrink:0;"
        >
          Get My Free Property Report
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
      </div>
    </div>

  </div>
</section>


<!-- ════════════════════════════════════════════
     CURATED LISTINGS
════════════════════════════════════════════ -->
<section style="background:var(--color-surface-0);padding:6rem 0 7rem;">
  <div class="max-w-[1400px] mx-auto px-8 lg:px-12">

    <!-- Header -->
    <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-6 mb-14">
      <div class="reveal">
        <span class="eyebrow mb-4">Available Now</span>
        <h2 style="font-family:var(--font-serif);font-size:clamp(2rem,4vw,2.75rem);font-weight:400;color:var(--color-text-primary);letter-spacing:-0.01em;line-height:1.1;">
          Daniel's Listings
        </h2>
        <p style="font-size:0.875rem;color:var(--color-text-secondary);margin-top:0.75rem;max-width:46ch;line-height:1.75;">
          Current properties for sale through Daniel across Brisbane's inner east and south.
        </p>
      </div>
      <a href="/listings" class="reveal" data-delay="100"
        style="font-family:var(--font-sans);font-size:0.68rem;font-weight:700;letter-spacing:0.14em;text-transform:uppercase;color:var(--color-gold);border-bottom:1px solid var(--color-gold);padding-bottom:2px;white-space:nowrap;transition:opacity 0.2s;flex-shrink:0;align-self:flex-end;">
        Explore All Properties
      </a>
    </div>

    <!-- Asymmetric grid -->
    <div class="grid grid-cols-1 md:grid-cols-12 gap-6">

      <!-- Large card col-8 -->
      <a href={listings[0].href} target="_blank" rel="noopener noreferrer"
        class="prop-card md:col-span-8 reveal" data-delay="0" style="display:block;text-decoration:none;">
        <div class="img-wrap" style="aspect-ratio:16/9;">
          <img src={listings[0].image} alt={listings[0].address} width="1200" height="675" loading="eager" />
          <div class="absolute top-5 left-5"><span class="gold-pill">{listings[0].tag}</span></div>
        </div>
        <div style="padding:1.5rem 0 1.75rem;">
          <div class="flex items-start justify-between gap-4 mb-2">
            <div>
              <span style="font-size:0.6rem;font-weight:700;letter-spacing:0.18em;text-transform:uppercase;color:var(--color-gold);">{listings[0].suburb}</span>
              <h3 style="font-family:var(--font-serif);font-size:1.6rem;font-weight:400;color:var(--color-text-primary);margin-top:0.2rem;">{listings[0].address}</h3>
              <p style="font-size:0.75rem;color:var(--color-text-muted);margin-top:0.2rem;">{listings[0].street}</p>
            </div>
            <span style="font-family:var(--font-sans);font-size:0.7rem;font-weight:700;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-gold);margin-top:0.5rem;white-space:nowrap;">
              {listings[0].price}
            </span>
          </div>
          <div style="display:flex;gap:1.25rem;margin-top:0.75rem;padding-top:0.75rem;border-top:1px solid rgba(28,25,23,0.1);">
            <span style="font-size:0.68rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{listings[0].beds} Bed</span>
            <span style="font-size:0.68rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{listings[0].baths} Bath</span>
            <span style="font-size:0.68rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{listings[0].cars} Car</span>
          </div>
        </div>
      </a>

      <!-- Tall card col-4 -->
      <a href={listings[1].href} target="_blank" rel="noopener noreferrer"
        class="prop-card md:col-span-4 reveal" data-delay="100" style="display:block;text-decoration:none;">
        <div class="img-wrap" style="aspect-ratio:4/5;">
          <img src={listings[1].image} alt={listings[1].address} width="800" height="1000" loading="lazy" />
          <div class="absolute top-5 left-5"><span class="gold-pill">{listings[1].tag}</span></div>
        </div>
        <div style="padding:1.25rem 0 1.5rem;">
          <span style="font-size:0.6rem;font-weight:700;letter-spacing:0.18em;text-transform:uppercase;color:var(--color-gold);">{listings[1].suburb}</span>
          <h3 style="font-family:var(--font-serif);font-size:1.35rem;font-weight:400;color:var(--color-text-primary);margin-top:0.2rem;">{listings[1].address}</h3>
          <div style="display:flex;gap:1rem;margin-top:0.6rem;">
            <span style="font-size:0.68rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{listings[1].beds} Bed</span>
            <span style="font-size:0.68rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{listings[1].baths} Bath</span>
            <span style="font-size:0.68rem;font-weight:700;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-gold);">{listings[1].price}</span>
          </div>
        </div>
      </a>

      <!-- Square cards -->
      {listings.slice(2).map((p, i) => (
        <a href={p.href} target="_blank" rel="noopener noreferrer"
          class="prop-card md:col-span-4 reveal" data-delay={`${(i+1)*80}`} style="display:block;text-decoration:none;">
          <div class="img-wrap" style="aspect-ratio:1/1;">
            <img src={p.image} alt={p.address} width="600" height="600" loading="lazy" />
            <div class="absolute top-5 left-5"><span class="gold-pill">{p.tag}</span></div>
          </div>
          <div style="padding:1rem 0 1.25rem;">
            <span style="font-size:0.6rem;font-weight:700;letter-spacing:0.18em;text-transform:uppercase;color:var(--color-gold);">{p.suburb}</span>
            <h3 style="font-family:var(--font-serif);font-size:1.15rem;font-weight:400;color:var(--color-text-primary);margin-top:0.2rem;">{p.address}</h3>
            <p style="font-size:0.68rem;font-weight:700;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-gold);margin-top:0.4rem;">{p.price}</p>
          </div>
        </a>
      ))}

    </div>
  </div>
</section>


<!-- ════════════════════════════════════════════
     PRESTIGE COLLECTION
════════════════════════════════════════════ -->
<section style="background:var(--color-surface-2);padding:6rem 0 7rem;">
  <div class="max-w-[1400px] mx-auto px-8 lg:px-12">

    <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-6 mb-14">
      <div class="reveal">
        <span class="eyebrow mb-4">Ray White Collective</span>
        <h2 style="font-family:var(--font-serif);font-size:clamp(2rem,4vw,2.75rem);font-weight:400;color:var(--color-text-primary);letter-spacing:-0.01em;line-height:1.1;">
          Collective Listings
        </h2>
        <p style="font-size:0.875rem;color:var(--color-text-secondary);margin-top:0.75rem;max-width:48ch;line-height:1.75;">
          More from the Ray White Bulimba office — and the broader Ray White network across Brisbane.
        </p>
      </div>
      <a href="https://raywhitebulimba.com.au" target="_blank" rel="noopener noreferrer" class="btn-outline-gold reveal self-end" data-delay="100">View All Listings</a>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
      {prestige.map((p, i) => (
        <a href={p.href} target="_blank" rel="noopener noreferrer"
          class={`prop-card reveal${i === 1 ? '-right' : '-left'}`} data-delay={`${i * 80}`}
          style="display:block;text-decoration:none;background:var(--color-surface-0);">
          <div class="img-wrap relative" style="aspect-ratio:3/2;">
            <img src={p.image} alt={p.address} width="1200" height="800" loading="lazy"
              style="filter:grayscale(25%) brightness(0.75);" />
            <div class="absolute top-5 left-5"><span class="gold-pill">{p.tag}</span></div>
            <div class="absolute inset-0 pointer-events-none"
              style="background:linear-gradient(to top,rgba(14,14,14,0.92) 0%,transparent 55%);"></div>
          </div>
          <div style="padding:1.75rem;">
            <span style="font-size:0.6rem;font-weight:700;letter-spacing:0.18em;text-transform:uppercase;color:var(--color-gold);display:block;margin-bottom:0.4rem;">{p.suburb}</span>
            <h3 style="font-family:var(--font-serif);font-size:1.5rem;font-weight:400;color:var(--color-text-primary);margin-bottom:0.6rem;">{p.address}</h3>
            <p style="font-size:0.825rem;color:var(--color-text-secondary);line-height:1.75;margin-bottom:1.25rem;max-width:52ch;">{p.desc}</p>
            <div style="display:flex;align-items:center;justify-content:space-between;padding-top:1rem;border-top:1px solid rgba(28,25,23,0.1);">
              <div style="display:flex;gap:1.25rem;">
                <span style="font-size:0.65rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{p.beds} Bed</span>
                <span style="font-size:0.65rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{p.baths} Bath</span>
                <span style="font-size:0.65rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-muted);">{p.cars} Car</span>
              </div>
              <span style="font-size:0.75rem;font-weight:700;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-gold);">{p.price}</span>
            </div>
          </div>
        </a>
      ))}
    </div>

    <!-- CTA block -->
    <div class="reveal mt-6 p-10 lg:p-12 flex flex-col lg:flex-row items-start lg:items-center justify-between gap-6"
      style="background:var(--color-gold-muted);border:1px solid rgba(196,145,42,0.15);" data-delay="100">
      <div>
        <h3 style="font-family:var(--font-serif);font-size:1.5rem;font-weight:400;color:var(--color-text-primary);margin-bottom:0.5rem;font-style:italic;">
          Looking for Something Specific?
        </h3>
        <p style="font-size:0.85rem;color:var(--color-text-muted);max-width:52ch;line-height:1.75;">
          Daniel's network spans all Ray White Bulimba listings and the broader Ray White Collective. Tell him what you're looking for.
        </p>
      </div>
      <a href="tel:+61412523821" class="btn-gold" style="flex-shrink:0;">
        Call Daniel
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.61 3.39 2 2 0 0 1 3.6 1.21h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.83a16 16 0 0 0 5.92 5.92l.91-.91a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 21.21 16z"/></svg>
      </a>
    </div>
  </div>
</section>


<!-- ════════════════════════════════════════════
     WHY CHOOSE US
════════════════════════════════════════════ -->
<section style="background:var(--color-surface-1);padding:7rem 0;overflow:hidden;">
  <div class="max-w-[1400px] mx-auto px-8 lg:px-12">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 lg:gap-24 items-center">

      <!-- Portrait + quote card -->
      <div class="relative reveal-left">
        <div style="aspect-ratio:3/4;overflow:hidden;border:1px solid rgba(28,25,23,0.1);">
          <img
            src="https://cdn6.ep.dynamics.net/s3/rw-media/memberphotos/88889915-cef5-4a5f-9c19-0cea700d7bca.jpeg"
            alt="Daniel Gierach — Ray White Bulimba"
            class="w-full h-full object-cover"
            loading="lazy" width="900" height="1200"
          />
        </div>
        <!-- Overlapping quote -->
        <div class="hidden lg:block absolute -bottom-10 -right-10 max-w-xs p-8"
          style="background:var(--color-base);border:1px solid rgba(28,25,23,0.12);box-shadow:0 8px 32px rgba(28,25,23,0.1);">
          <p style="font-family:var(--font-serif);font-size:1.05rem;font-style:italic;color:var(--color-text-primary);line-height:1.65;margin-bottom:1rem;">
            "Brisbane's inner east isn't just a location — it's a way of life."
          </p>
          <span style="font-family:var(--font-sans);font-size:0.6rem;font-weight:700;letter-spacing:0.2em;text-transform:uppercase;color:var(--color-gold);">
            Daniel Gierach · Ray White Bulimba
          </span>
          <div style="margin-top:0.75rem;padding-top:0.75rem;border-top:1px solid rgba(28,25,23,0.1);">
            <a href="tel:+61412523821" style="font-family:var(--font-sans);font-size:0.7rem;font-weight:700;color:var(--color-gold);letter-spacing:0.06em;text-decoration:none;">
              0412 523 821
            </a>
          </div>
        </div>
      </div>

      <!-- Content -->
      <div class="reveal-right">
        <span class="eyebrow mb-5">Why Choose Daniel</span>
        <h2 style="font-family:var(--font-serif);font-size:clamp(2rem,4vw,2.75rem);font-weight:400;color:var(--color-text-primary);letter-spacing:-0.01em;line-height:1.15;margin-bottom:2.5rem;">
          Brisbane's Inner East<br /><em style="font-style:italic;">Property Specialist</em>
        </h2>

        <div class="flex flex-col gap-10">
          {reasons.map((r, i) => (
            <div class="flex gap-6 reveal" data-delay={`${i * 100}`}>
              <span style="font-family:var(--font-serif);font-size:2rem;font-weight:400;color:var(--color-gold);opacity:0.35;flex-shrink:0;line-height:1;margin-top:0.2rem;">
                {r.n}
              </span>
              <div>
                <h4 style="font-family:var(--font-sans);font-size:0.8rem;font-weight:800;letter-spacing:0.1em;text-transform:uppercase;color:var(--color-text-primary);margin-bottom:0.6rem;">
                  {r.title}
                </h4>
                <p style="font-size:0.875rem;color:var(--color-text-secondary);line-height:1.8;">
                  {r.body}
                </p>
              </div>
            </div>
          ))}
        </div>

        <div class="mt-10 reveal" data-delay="300">
          <a href="/about" class="btn-outline-gold">About Daniel</a>
        </div>
      </div>
    </div>
  </div>
</section>



<!-- ════════════════════════════════════════════
     APPRAISAL CTA
════════════════════════════════════════════ -->
<section class="relative overflow-hidden" style="padding:7rem 0;">
  <div class="absolute inset-0">
    <img
      src="https://images.unsplash.com/photo-1600566753190-17f0baa2a6c3?w=1920&q=80"
      alt="Premium property"
      class="w-full h-full object-cover"
      style="filter:grayscale(40%) brightness(0.28);"
      loading="lazy" width="1920" height="1080"
    />
    <div class="absolute inset-0" style="background:rgba(14,14,14,0.6);"></div>
  </div>

  <div class="relative max-w-[1400px] mx-auto px-8 lg:px-12 text-center">
    <span class="eyebrow mb-5 reveal" style="display:block;text-align:center;">What's Your Home Worth?</span>
    <h2 class="reveal" data-delay="80"
      style="font-family:var(--font-serif);font-size:clamp(2.25rem,5vw,3.75rem);font-weight:400;color:var(--color-text-primary);letter-spacing:-0.02em;line-height:1.1;margin-bottom:1.25rem;">
      Request a Free Property Appraisal
    </h2>
    <p class="reveal" data-delay="160"
      style="font-size:0.9375rem;color:var(--color-text-secondary);max-width:44ch;margin:0 auto 2.5rem;line-height:1.8;">
      Daniel will assess your property's true market value with no obligation and no pressure.
    </p>
    <div class="flex flex-wrap gap-4 justify-center reveal" data-delay="240">
      <a href="/appraisal" class="btn-gold">
        Request an Appraisal
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </a>
      <a href="tel:+61412523821" class="btn-ghost-light">Call Daniel · 0412 523 821</a>
    </div>
  </div>
</section>


<!-- ════════════════════════════════════════════
     TOOLS SHOWCASE
════════════════════════════════════════════ -->
<section style="background:var(--color-surface-1);padding:6rem 0;border-top:1px solid rgba(72,72,72,0.15);">
  <div class="max-w-[1400px] mx-auto px-8 lg:px-12">

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-end mb-12">
      <div>
        <span class="eyebrow mb-4 reveal" style="display:block;">Resources</span>
        <h2 class="reveal" data-delay="80"
          style="font-family:var(--font-serif);font-size:clamp(2rem,3.5vw,2.75rem);font-weight:400;color:var(--color-text-primary);letter-spacing:-0.01em;line-height:1.15;">
          Free Property Tools<br />& Calculators
        </h2>
      </div>
      <div>
        <p class="reveal" data-delay="120" style="font-size:0.9rem;color:var(--color-text-secondary);line-height:1.8;max-width:46ch;">
          Make smarter decisions at every stage of your property journey. Free tools built for buying, selling, and investing across Brisbane's inner east and south.
        </p>
        <a href="/tools" class="btn-outline-gold reveal" data-delay="200" style="display:inline-flex;margin-top:1.5rem;">
          View All Tools →
        </a>
      </div>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
      {showcaseTools.map((tool, i) => (
        <a href={tool.href}
          class="reveal"
          data-delay={String((i % 4) * 60)}
          style="background:var(--color-surface-3);padding:1.75rem;display:flex;flex-direction:column;gap:0.75rem;text-decoration:none;transition:background 0.25s,transform 0.25s;"
          onmouseover="this.style.background='var(--color-surface-4)';this.style.transform='translateY(-4px)'"
          onmouseout="this.style.background='var(--color-surface-3)';this.style.transform='translateY(0)'"
        >
          <span style={`font-size:0.5rem;font-weight:800;letter-spacing:0.2em;text-transform:uppercase;color:${toolPhaseColors[tool.phase] || '#ffdd79'};`}>
            {tool.phase}
          </span>
          <h3 style="font-family:var(--font-serif);font-size:1.05rem;font-weight:400;color:var(--color-text-primary);">{tool.label}</h3>
          <p style="font-size:0.75rem;color:var(--color-text-muted);line-height:1.65;flex:1;">{tool.desc}</p>
          <span style="font-size:0.6rem;font-weight:700;letter-spacing:0.12em;text-transform:uppercase;color:var(--color-gold);">Use Tool →</span>
        </a>
      ))}
    </div>
  </div>
</section>


<!-- ════════════════════════════════════════════
     SCRIPTS
════════════════════════════════════════════ -->
<script>
  // ── Tab switching ──
  const tabs = document.querySelectorAll<HTMLButtonElement>('.s-tab');
  const panels = document.querySelectorAll<HTMLElement>('.search-panel');

  tabs.forEach(tab => {
    tab.addEventListener('click', () => {
      tabs.forEach(t => t.classList.remove('active'));
      tab.classList.add('active');
      const target = tab.dataset.tab!;
      panels.forEach(p => (p.style.display = 'none'));
      const active = document.getElementById(`panel-${target}`);
      if (active) active.style.display = 'block';
    });
  });

  // ── Hero parallax ──
  const heroBg = document.getElementById('hero-bg');
  if (heroBg) {
    window.addEventListener('scroll', () => {
      const y = window.scrollY;
      heroBg.style.transform = `translateY(${y * 0.3}px)`;
    }, { passive: true });
  }

  // ── Stat counters ──
  const counters = document.querySelectorAll<HTMLElement>('.stat-counter');
  const countObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      const el = entry.target as HTMLElement;
      const target = parseFloat(el.dataset.value!);
      const suffix = el.dataset.suffix || '';
      const prefix = el.dataset.prefix || '';
      const isDecimal = String(target).includes('.');
      const duration = 1800;
      const start = performance.now();

      const tick = (now: number) => {
        const elapsed = now - start;
        const progress = Math.min(elapsed / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        const current = target * eased;
        el.textContent = prefix + (isDecimal ? current.toFixed(1) : Math.round(current).toLocaleString()) + suffix;
        if (progress < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
      countObserver.unobserve(el);
    });
  }, { threshold: 0.5 });

  counters.forEach(el => countObserver.observe(el));
</script>

</Layout>
