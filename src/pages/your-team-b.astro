---
import LandingLayout from '../layouts/LandingLayout.astro';

// Tools for a social-media audience. Property Report goes through the
// address-first chooser at /report (then on to /property-worth's three
// options). Off-market is the buyer-side capture.
const services = [
  {
    href: '/report',
    title: 'Your Property Report',
    desc: 'Online, in-person, or by phone. Your call.',
    icon: 'report',
  },
  {
    href: '/buyers',
    title: 'Off-market & pre-market access',
    desc: 'See homes before they hit realestate.com.au.',
    icon: 'key',
  },
];

const articles = [
  {
    href: '/insights/aml-changes-real-estate-brisbane-1-july-2026',
    title: 'New AML rules from 1 July 2026',
    desc: 'What changes for buyers and sellers.',
    icon: 'shield',
  },
  {
    href: '/insights/seller-disclosure-obligations-queensland',
    title: 'Seller disclosure obligations in Queensland',
    desc: 'What a vendor must (and should) tell a buyer.',
    icon: 'doc',
  },
];

const agents = [
  {
    name: 'Daniel Gierach',
    slug: 'daniel-gierach',
    photo: '/img/daniel-headshot-tight.jpg',
    phone: '0412 523 821',
    tel: '+61412523821',
    email: 'daniel.gierach@raywhite.com',
    handle: 'dgierach',
    instagramUrl: 'https://www.instagram.com/dgierach/',
    ctaKey: 'daniel',
  },
  {
    name: 'Conrad Leisemann',
    slug: 'conrad-leisemann',
    photo: '/img/conrad-headshot.jpg',
    phone: '0403 978 813',
    tel: '+61403978813',
    email: 'conrad.leisemann@raywhite.com',
    handle: 'connie_leisemann',
    instagramUrl: 'https://www.instagram.com/connie_leisemann/',
    ctaKey: 'conrad',
  },
];

---

<LandingLayout
  title="Your Team | Daniel Gierach & Conrad Leisemann"
  description="Daniel Gierach and Conrad Leisemann at Ray White Bulimba. Inner-east Brisbane buyer and seller tools, a free property report, and off-market access."
>

<style>
  :root {
    --bg: #0a0806;
    --bg-soft: #100c08;
    --card: #15110d;
    --card-hover: #1c1612;
    --line: rgba(240, 236, 228, 0.07);
    --line-strong: rgba(196, 145, 42, 0.32);
    --line-glow: rgba(196, 145, 42, 0.5);
    --text: #f2efe9;
    --text-mute: rgba(242, 239, 233, 0.5);
    --text-soft: rgba(242, 239, 233, 0.78);
    --gold: #c4912a;
    --gold-bright: #d4a23a;
    --gold-soft: rgba(196, 145, 42, 0.85);
  }

  body { background: var(--bg); color: var(--text); position: relative; }

  /* Header background photo: full-bleed, dark, fades to solid charcoal
     before the first card so it never competes with content. */
  .hero-bg {
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 380px;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }
  .hero-bg img {
    width: 100%; height: 100%;
    object-fit: cover; object-position: center 92%;
    filter: grayscale(12%) brightness(0.62) contrast(1.05);
  }
  .hero-bg::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 80% 60% at 50% 0%, rgba(196,145,42,0.1) 0%, rgba(196,145,42,0) 70%),
      linear-gradient(
        180deg,
        rgba(10,8,6,0.55) 0%,
        rgba(10,8,6,0.5) 28%,
        rgba(10,8,6,0.7) 55%,
        rgba(10,8,6,0.88) 80%,
        var(--bg) 100%
      );
  }
  @media (min-width: 640px) { .hero-bg { height: 420px; } }

  .shell {
    max-width: 480px;
    margin: 0 auto;
    padding: 1.5rem 1.15rem 2.5rem;
    min-height: 100svh;
    position: relative;
    z-index: 1;
  }
  @media (min-width: 640px) {
    .shell { padding: 2rem 1.5rem 3rem; }
  }

  /* ── Header (two-agent: Daniel + Conrad) ─────────────────────── */
  .header { text-align: center; margin-bottom: 1.5rem; }

  .agency-eyebrow {
    font-family: var(--font-sans);
    font-size: 0.56rem; font-weight: 700;
    letter-spacing: 0.22em; text-transform: uppercase;
    color: var(--gold-soft);
    margin-bottom: 0.5rem;
  }

  .team-title {
    font-family: var(--font-serif);
    font-size: 1.25rem; font-weight: 400;
    font-style: italic;
    color: var(--text);
    letter-spacing: -0.01em;
    margin-bottom: 1.4rem;
    text-shadow: 0 1px 12px rgba(0,0,0,0.6);
  }

  .agents {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 0;
  }
  @media (min-width: 480px) { .agents { gap: 1.5rem; } }

  .agent {
    display: flex; flex-direction: column; align-items: center;
    min-width: 0;
  }

  .avatar {
    width: 76px; height: 76px;
    border-radius: 14px;
    object-fit: cover; object-position: center 35%;
    display: block;
    margin: 0 0 0.5rem;
    border: 1.5px solid rgba(197, 160, 60, 0.55);
    box-shadow: 0 6px 22px rgba(0, 0, 0, 0.55), 0 0 0 4px rgba(196, 145, 42, 0.05);
  }
  @media (min-width: 480px) { .avatar { width: 88px; height: 88px; } }

  .name {
    font-family: var(--font-serif);
    font-size: 1rem; font-weight: 700;
    line-height: 1.15; letter-spacing: 0.005em;
    margin-bottom: 0.3rem;
    min-height: 2.4em;
    display: flex; align-items: center; justify-content: center;
    text-align: center;
    text-shadow: 0 1px 12px rgba(0,0,0,0.65), 0 1px 2px rgba(0,0,0,0.5);
  }
  @media (min-width: 480px) { .name { font-size: 1.15rem; min-height: 1.3em; } }

  /* Stack phone / handle / email vertically so both columns always have
     the same number of lines, keeping the two cards aligned no matter
     how long the handles or addresses are. */
  .contact-line {
    display: flex; flex-direction: column; align-items: center;
    gap: 0.1rem;
    font-family: var(--font-sans);
    font-size: 0.7rem; font-weight: 700;
    line-height: 1.25;
    margin-bottom: 0.15rem;
    text-shadow: 0 1px 8px rgba(0,0,0,0.6), 0 1px 2px rgba(0,0,0,0.45);
  }
  @media (min-width: 480px) { .contact-line { font-size: 0.78rem; } }
  .contact-line a { color: var(--gold); text-decoration: none; transition: color 0.2s; white-space: nowrap; }
  .contact-line a:hover { color: var(--gold-bright); }
  .contact-line .sep { display: none; }
  .handle-text { color: rgba(242,239,233,0.78); font-weight: 600; }
  .handle-text a { color: rgba(242,239,233,0.78); }
  .handle-text a:hover { color: var(--gold); }

  .email {
    font-family: var(--font-sans);
    font-size: 0.6rem; font-weight: 500;
    line-height: 1.25;
    word-break: break-all;
    text-shadow: 0 1px 6px rgba(0,0,0,0.55);
    padding: 0 0.25rem;
    margin: 0;
  }
  @media (min-width: 480px) { .email { font-size: 0.66rem; word-break: normal; } }
  .email a {
    color: rgba(242,239,233,0.85); text-decoration: none;
    transition: color 0.2s;
  }
  .email a:hover { color: var(--gold); }

  /* "More info" button under each primary agent card (links to /team/{slug}) */
  .agent-info-btn {
    display: inline-flex; align-items: center; justify-content: center;
    gap: 0.3rem;
    margin-top: 0.65rem;
    padding: 0.4rem 0.75rem;
    background: rgba(196, 145, 42, 0.08);
    border: 1px solid rgba(196, 145, 42, 0.35);
    border-radius: 6px;
    font-family: var(--font-sans);
    font-size: 0.52rem; font-weight: 800;
    letter-spacing: 0.12em; text-transform: uppercase;
    color: var(--gold);
    text-decoration: none;
    transition: background 0.18s, border-color 0.18s, color 0.18s;
    white-space: nowrap;
  }
  @media (min-width: 480px) { .agent-info-btn { font-size: 0.55rem; padding: 0.45rem 0.9rem; } }
  .agent-info-btn:hover {
    background: rgba(196, 145, 42, 0.18);
    border-color: var(--gold);
    color: var(--gold-bright);
  }

  /* ── Wider team (Samantha, Haesley, Peter) ────────────────────
     Centered row of equal-width cards. align-items:stretch via flex
     gives every card the same height so the "More info" button at
     the bottom of each card lines up across the row. */
  .wider-team-label {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.56rem; font-weight: 800;
    letter-spacing: 0.22em; text-transform: uppercase;
    color: var(--text-mute);
    text-align: center;
    margin-top: 2rem; margin-bottom: 1rem;
  }
  .wider-team {
    display: flex;
    justify-content: center;
    gap: 0.7rem;
    margin-bottom: 1.5rem;
  }
  @media (min-width: 480px) { .wider-team { gap: 1rem; } }
  .wt-card {
    display: flex; flex-direction: column; align-items: center;
    padding: 0.9rem 0.5rem 0.85rem;
    background: rgba(15, 11, 7, 0.55);
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(196, 145, 42, 0.18);
    border-radius: 8px;
    transition: background 0.18s, border-color 0.18s;
    /* Equal width siblings; cap at 200px so the row stays compact */
    flex: 1 1 0;
    min-width: 0;
    max-width: 200px;
  }
  .wt-card:hover { background: rgba(20, 14, 9, 0.75); border-color: rgba(196, 145, 42, 0.4); }
  .wt-avatar {
    width: 60px; height: 60px;
    border-radius: 10px;
    object-fit: cover; object-position: center 22%;
    border: 1px solid rgba(197, 160, 60, 0.45);
    margin-bottom: 0.6rem;
  }
  @media (min-width: 480px) { .wt-avatar { width: 70px; height: 70px; border-radius: 12px; } }
  .wt-name {
    font-family: var(--font-serif);
    font-size: 0.78rem; font-weight: 700;
    line-height: 1.2;
    text-align: center;
    margin-bottom: 0.2rem;
    color: var(--text);
    text-shadow: 0 1px 8px rgba(0,0,0,0.55);
    /* Reserve 2 lines so cards stay the same height even if a name fits
       on one line (e.g. Peter Burgin) vs wraps (Samantha Squires) */
    min-height: 2.4em;
    display: flex; align-items: center;
  }
  @media (min-width: 480px) { .wt-name { font-size: 0.88rem; min-height: 1.2em; } }
  .wt-role {
    font-family: var(--font-sans);
    font-size: 0.5rem; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--gold-soft);
    text-align: center;
    margin-bottom: 0.7rem;
    line-height: 1.3;
    min-height: 2.6em;
    display: flex; align-items: center; justify-content: center;
  }
  @media (min-width: 480px) { .wt-role { font-size: 0.55rem; } }
  .wt-info-btn {
    display: inline-flex; align-items: center; justify-content: center;
    gap: 0.3rem;
    padding: 0.5rem 0.8rem;
    background: rgba(196, 145, 42, 0.08);
    border: 1px solid rgba(196, 145, 42, 0.35);
    border-radius: 6px;
    font-family: var(--font-sans);
    font-size: 0.52rem; font-weight: 800;
    letter-spacing: 0.12em; text-transform: uppercase;
    color: var(--gold);
    text-decoration: none;
    transition: background 0.18s, border-color 0.18s, color 0.18s;
    margin-top: auto;
    white-space: nowrap;
  }
  @media (min-width: 480px) { .wt-info-btn { font-size: 0.55rem; padding: 0.55rem 1rem; } }
  .wt-info-btn:hover {
    background: rgba(196, 145, 42, 0.18);
    border-color: var(--gold);
    color: var(--gold-bright);
  }

  /* ── Current listings CTA (gold button above tools) ──────────── */
  .listings-cta {
    display: flex; align-items: center; justify-content: center;
    gap: 0.6rem;
    max-width: 320px;
    margin: 0 auto 1.5rem;
    padding: 0.95rem 1.5rem;
    background: var(--gold);
    color: var(--bg);
    border-radius: 8px;
    text-decoration: none;
    font-family: var(--font-sans);
    font-size: 0.72rem; font-weight: 800;
    letter-spacing: 0.14em; text-transform: uppercase;
    transition: background 0.2s, transform 0.18s, box-shadow 0.2s;
    box-shadow: 0 8px 22px rgba(196,145,42,0.28);
  }
  .listings-cta:hover { background: var(--gold-bright); transform: translateY(-1px); box-shadow: 0 10px 28px rgba(196,145,42,0.38); }
  .listings-cta:active { transform: translateY(0); }
  .listings-cta-icon { display: inline-flex; }
  .listings-cta-text { white-space: nowrap; }
  .listings-cta-arrow { transition: transform 0.18s; }
  .listings-cta:hover .listings-cta-arrow { transform: translateX(2px); }

  /* ── Section labels ───────────────────────────────────────────── */
  .section { margin-bottom: 1.5rem; }
  .section-label {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.58rem; font-weight: 800;
    letter-spacing: 0.2em; text-transform: uppercase;
    color: var(--text-mute);
    margin-bottom: 0.7rem; padding-left: 0.2rem;
  }

  /* ── Service cards (with icon) ────────────────────────────────── */
  .card {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 0.95rem;
    background:
      linear-gradient(135deg, rgba(196,145,42,0.04) 0%, rgba(196,145,42,0) 60%),
      var(--card);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 0.95rem 1.05rem;
    text-decoration: none;
    color: var(--text);
    margin-bottom: 0.55rem;
    transition: background 0.2s, border-color 0.2s, transform 0.18s;
    position: relative;
    overflow: hidden;
  }
  .card::before {
    content: '';
    position: absolute; left: 0; top: 0; bottom: 0;
    width: 2px; background: var(--gold);
    opacity: 0.6;
    transition: opacity 0.2s, width 0.2s;
  }
  .card:hover, .card:focus-visible {
    background:
      linear-gradient(135deg, rgba(196,145,42,0.08) 0%, rgba(196,145,42,0.01) 60%),
      var(--card-hover);
    border-color: var(--line-strong);
    transform: translateY(-1px);
  }
  .card:hover::before { opacity: 1; width: 3px; }
  .card:active { transform: translateY(0); }

  .card-icon {
    width: 36px; height: 36px;
    display: flex; align-items: center; justify-content: center;
    background: rgba(196, 145, 42, 0.1);
    border: 1px solid rgba(196, 145, 42, 0.18);
    border-radius: 50%;
    color: var(--gold-bright);
    flex-shrink: 0;
  }

  .card-body { min-width: 0; }
  .card-eyebrow {
    font-family: var(--font-sans);
    font-size: 0.52rem; font-weight: 800;
    letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--gold);
    display: block; margin-bottom: 0.18rem;
  }
  .card-title {
    font-family: var(--font-serif);
    font-size: 1rem; font-weight: 400;
    line-height: 1.2;
    margin-bottom: 0.2rem;
  }
  .card-desc {
    font-size: 0.7rem; line-height: 1.4;
    color: var(--text-soft);
  }
  .card-arrow {
    color: var(--text-mute);
    flex-shrink: 0;
    transition: color 0.2s, transform 0.2s;
  }
  .card:hover .card-arrow {
    color: var(--gold);
    transform: translateX(2px);
  }

  /* ── Article cards (smaller, icon-led) ────────────────────────── */
  .acard {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 0.85rem;
    padding: 0.7rem 0.95rem;
    background: transparent;
    border: 1px solid var(--line);
    border-radius: 10px;
    text-decoration: none;
    color: var(--text);
    margin-bottom: 0.45rem;
    transition: background 0.2s, border-color 0.2s;
  }
  .acard:hover, .acard:focus-visible {
    background: var(--bg-soft);
    border-color: var(--line-strong);
  }
  .acard-icon {
    width: 28px; height: 28px;
    display: flex; align-items: center; justify-content: center;
    color: var(--gold-soft);
    flex-shrink: 0;
  }
  .acard-body { min-width: 0; }
  .acard-title {
    font-family: var(--font-serif);
    font-size: 0.88rem; font-weight: 400;
    line-height: 1.25;
    margin-bottom: 0.15rem;
  }
  .acard-desc {
    font-size: 0.66rem; line-height: 1.35;
    color: var(--text-mute);
  }

  .browse-all {
    display: flex; align-items: center; justify-content: center; gap: 0.35rem;
    margin-top: 0.6rem; padding: 0.3rem 0.2rem;
    font-family: var(--font-sans);
    font-size: 0.6rem; font-weight: 800;
    letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--gold); text-decoration: none;
    transition: color 0.18s;
  }
  .browse-all:hover { color: var(--gold-bright); }

  /* ── Final CTA panel ──────────────────────────────────────────── */
  .final {
    background:
      linear-gradient(135deg, rgba(196,145,42,0.15) 0%, rgba(196,145,42,0.04) 100%);
    border: 1px solid var(--line-strong);
    border-radius: 12px;
    padding: 1.75rem 1.25rem;
    margin-top: 1.5rem;
    text-align: center;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
  }
  .final > * { margin: 0; }
  .final-title {
    font-family: var(--font-serif);
    font-size: 0.98rem; font-weight: 400; line-height: 1.3;
  }
  .final-sub {
    font-size: 0.7rem; line-height: 1.5;
    color: var(--text-soft);
  }
  .btn-gold {
    display: inline-flex; align-items: center; gap: 0.4rem;
    padding: 0.75rem 1.25rem;
    background: var(--gold); color: var(--bg);
    font-family: var(--font-sans);
    font-size: 0.66rem; font-weight: 800;
    letter-spacing: 0.14em; text-transform: uppercase;
    text-decoration: none;
    border-radius: 8px;
    transition: background 0.2s, transform 0.18s;
  }
  .btn-gold:hover { background: var(--gold-bright); transform: translateY(-1px); }

  /* ── Bottom row ──────────────────────────────────────────────── */
  .contact-row {
    display: flex; justify-content: center; gap: 1.25rem;
    margin-top: 1.25rem; padding-top: 1rem;
    border-top: 1px solid var(--line);
  }
  .contact-row a {
    font-family: var(--font-sans);
    font-size: 0.6rem; font-weight: 700;
    letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--text-mute); text-decoration: none;
    transition: color 0.18s;
  }
  .contact-row a:hover { color: var(--gold); }

  .legal {
    text-align: center;
    margin-top: 1rem;
    font-size: 0.56rem; color: var(--text-mute);
    line-height: 1.5;
    opacity: 0.85;
  }

  /* ── B: Trust strip at top ──────────────────────────────────── */
  .trust-strip {
    display: flex; align-items: center; justify-content: center;
    gap: 0.5rem; flex-wrap: wrap;
    padding: 0.55rem 0.9rem;
    margin-bottom: 1.1rem;
    border: 1px solid var(--line-strong);
    border-radius: 8px;
    background: rgba(196, 145, 42, 0.06);
    text-align: center;
  }
  .trust-stars { display: inline-flex; gap: 0.12rem; color: var(--gold-bright); }
  .trust-text {
    font-family: var(--font-sans);
    font-size: 0.56rem; font-weight: 700;
    letter-spacing: 0.14em; text-transform: uppercase;
    color: var(--text-soft);
  }
  @media (min-width: 480px) { .trust-text { font-size: 0.6rem; } }

  /* ── B: Primary CTA (Your Property Report) ──────────────────── */
  .primary-cta {
    display: grid;
    grid-template-columns: 1fr auto;
    grid-template-rows: auto auto auto;
    align-items: center;
    column-gap: 0.5rem;
    padding: 1.15rem 1.2rem;
    margin-bottom: 0.7rem;
    background:
      linear-gradient(135deg, var(--gold) 0%, #b88526 100%);
    color: #0a0806;
    border-radius: 14px;
    text-decoration: none;
    box-shadow: 0 14px 38px rgba(196,145,42,0.32), inset 0 1px 0 rgba(255,255,255,0.15);
    transition: transform 0.18s, box-shadow 0.2s;
  }
  .primary-cta:hover { transform: translateY(-1px); box-shadow: 0 18px 44px rgba(196,145,42,0.42), inset 0 1px 0 rgba(255,255,255,0.18); }
  .primary-cta:active { transform: translateY(0); }
  .pcta-eyebrow {
    grid-column: 1; grid-row: 1;
    font-family: var(--font-sans);
    font-size: 0.55rem; font-weight: 800;
    letter-spacing: 0.18em; text-transform: uppercase;
    color: rgba(10,8,6,0.7);
    margin-bottom: 0.25rem;
  }
  .pcta-title {
    grid-column: 1; grid-row: 2;
    font-family: var(--font-serif);
    font-size: 1.3rem; font-weight: 700;
    line-height: 1.15;
    color: #0a0806;
    margin-bottom: 0.2rem;
  }
  .pcta-sub {
    grid-column: 1; grid-row: 3;
    font-family: var(--font-sans);
    font-size: 0.74rem; font-weight: 500;
    line-height: 1.35;
    color: rgba(10,8,6,0.78);
  }
  .pcta-arrow {
    grid-column: 2; grid-row: 1 / 4;
    display: inline-flex; align-items: center; justify-content: center;
    width: 36px; height: 36px;
    background: rgba(10,8,6,0.12);
    border-radius: 50%;
    color: #0a0806;
    transition: transform 0.18s, background 0.2s;
  }
  .primary-cta:hover .pcta-arrow { background: rgba(10,8,6,0.2); transform: translateX(2px); }

  /* ── B: Secondary CTA (Current listings) ────────────────────── */
  .secondary-cta {
    display: flex; align-items: center; justify-content: center;
    gap: 0.5rem;
    padding: 0.85rem 1.1rem;
    margin-bottom: 1.1rem;
    border: 1px solid var(--line-strong);
    border-radius: 10px;
    background: transparent;
    color: var(--gold);
    text-decoration: none;
    font-family: var(--font-sans);
    font-size: 0.68rem; font-weight: 800;
    letter-spacing: 0.14em; text-transform: uppercase;
    transition: background 0.2s, border-color 0.2s, color 0.2s;
  }
  .secondary-cta:hover { background: rgba(196,145,42,0.06); border-color: var(--gold); color: var(--gold-bright); }

  /* ── B: Team strip (compact) ────────────────────────────────── */
  .ts-label {
    font-family: var(--font-sans);
    font-size: 0.58rem; font-weight: 800;
    letter-spacing: 0.2em; text-transform: uppercase;
    color: var(--text-mute);
    margin: 1.5rem 0 0.7rem;
    padding-left: 0.2rem;
  }
  .team-strip {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.6rem;
    margin-bottom: 1.5rem;
  }
  .ts-card {
    display: flex; align-items: center; gap: 0.7rem;
    padding: 0.7rem 0.75rem;
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: 10px;
    transition: background 0.18s, border-color 0.18s;
  }
  .ts-card:hover { background: var(--card-hover); border-color: var(--line-strong); }
  .ts-avatar {
    width: 48px; height: 48px;
    border-radius: 10px;
    object-fit: cover; object-position: center 18%;
    border: 1px solid rgba(197,160,60,0.4);
    flex-shrink: 0;
  }
  @media (min-width: 480px) { .ts-avatar { width: 56px; height: 56px; } }
  .ts-body { min-width: 0; flex: 1; }
  .ts-name {
    font-family: var(--font-serif);
    font-size: 0.85rem; font-weight: 700;
    line-height: 1.15;
    margin-bottom: 0.15rem;
    color: var(--text);
    white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
  }
  .ts-row { margin-bottom: 0.4rem; }
  .ts-call {
    font-family: var(--font-sans);
    font-size: 0.66rem; font-weight: 700;
    color: var(--gold); text-decoration: none;
    white-space: nowrap;
  }
  .ts-call:hover { color: var(--gold-bright); }
  .ts-more {
    display: inline-flex; align-items: center; gap: 0.25rem;
    padding: 0.3rem 0.55rem;
    background: rgba(196,145,42,0.08);
    border: 1px solid rgba(196,145,42,0.32);
    border-radius: 6px;
    font-family: var(--font-sans);
    font-size: 0.5rem; font-weight: 800;
    letter-spacing: 0.12em; text-transform: uppercase;
    color: var(--gold); text-decoration: none;
    transition: background 0.18s, border-color 0.18s, color 0.18s;
  }
  .ts-more:hover { background: rgba(196,145,42,0.16); border-color: var(--gold); color: var(--gold-bright); }

  /* ── B: Visual tile (replaces articles list) ────────────────── */
  .visual-tile {
    display: block;
    position: relative;
    margin-bottom: 1.5rem;
    border-radius: 12px;
    overflow: hidden;
    text-decoration: none;
    aspect-ratio: 16 / 9;
    background: var(--card);
    border: 1px solid var(--line);
    transition: transform 0.2s, border-color 0.2s;
  }
  .visual-tile:hover { transform: translateY(-1px); border-color: var(--line-strong); }
  .visual-tile img {
    width: 100%; height: 100%;
    object-fit: cover; object-position: center 65%;
    filter: brightness(0.6);
    transition: filter 0.25s, transform 0.4s;
  }
  .visual-tile:hover img { filter: brightness(0.7); transform: scale(1.02); }
  .vt-overlay {
    position: absolute;
    left: 1rem; right: 1rem; bottom: 0.9rem;
    color: var(--text);
  }
  .vt-eyebrow {
    font-family: var(--font-sans);
    font-size: 0.55rem; font-weight: 800;
    letter-spacing: 0.2em; text-transform: uppercase;
    color: var(--gold-bright);
    margin-bottom: 0.25rem;
    text-shadow: 0 1px 8px rgba(0,0,0,0.7);
  }
  .vt-title {
    font-family: var(--font-serif);
    font-size: 1.1rem; font-weight: 400;
    line-height: 1.15;
    color: #f6f1e6;
    text-shadow: 0 2px 12px rgba(0,0,0,0.7);
  }
  .vt-arrow {
    position: absolute; right: 0; bottom: 0;
    font-size: 1.1rem;
    color: var(--gold-bright);
    text-shadow: 0 1px 8px rgba(0,0,0,0.7);
  }

  /* ── B: Sticky mobile call bar ──────────────────────────────── */
  .mobile-call-bar { display: none; }
  @media (max-width: 480px) {
    .mobile-call-bar {
      display: flex; align-items: center; justify-content: center;
      gap: 0.5rem;
      position: fixed;
      left: 0.7rem; right: 0.7rem; bottom: 0.7rem;
      padding: 0.9rem 1rem;
      background: var(--gold);
      color: #0a0806;
      border-radius: 12px;
      font-family: var(--font-sans);
      font-size: 0.72rem; font-weight: 800;
      letter-spacing: 0.12em; text-transform: uppercase;
      text-decoration: none;
      box-shadow: 0 12px 32px rgba(0,0,0,0.4), 0 0 0 1px rgba(255,255,255,0.06);
      z-index: 50;
    }
    /* leave breathing room for the sticky bar above legal */
    .shell { padding-bottom: 5rem; }
  }
</style>

<!-- Header backdrop photo: heavily darkened, fades to solid charcoal
     so the page below stays clean and the cards do the talking. -->
<div class="hero-bg" aria-hidden="true">
  <img
    src="/_vercel/image?url=https%3A%2F%2Fcdn6.ep.dynamics.net%2Fs3%2Frw-propertyimages%2Ff0e3-H3512351-202426677__1779329460-52623-AMY9140.jpg&w=1080&q=65"
    srcset="
      /_vercel/image?url=https%3A%2F%2Fcdn6.ep.dynamics.net%2Fs3%2Frw-propertyimages%2Ff0e3-H3512351-202426677__1779329460-52623-AMY9140.jpg&w=640&q=65 640w,
      /_vercel/image?url=https%3A%2F%2Fcdn6.ep.dynamics.net%2Fs3%2Frw-propertyimages%2Ff0e3-H3512351-202426677__1779329460-52623-AMY9140.jpg&w=1080&q=65 1080w,
      /_vercel/image?url=https%3A%2F%2Fcdn6.ep.dynamics.net%2Fs3%2Frw-propertyimages%2Ff0e3-H3512351-202426677__1779329460-52623-AMY9140.jpg&w=1440&q=65 1440w
    "
    sizes="(max-width: 640px) 100vw, 1440px"
    alt=""
    fetchpriority="high"
  />
</div>

<div class="shell">

  <!-- B: Trust strip at the very top (above any action). One short line of
       geo + rating. Builds enough credibility to justify the click below. -->
  <div class="trust-strip" aria-label="Inner east Brisbane specialists, 5 stars">
    <span class="trust-stars" aria-hidden="true">
      {[1,2,3,4,5].map(() => (
        <svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      ))}
    </span>
    <span class="trust-text">Inner east Brisbane &middot; Bulimba, Hawthorne, Camp Hill, Morningside</span>
  </div>

  <!-- B: Primary action FIRST. Big gold panel for "Your Property Report" -->
  <a href="/report" class="primary-cta" data-cta="your_team_b_property_report">
    <span class="pcta-eyebrow">Free &middot; Takes 1 minute</span>
    <span class="pcta-title">Your Property Report</span>
    <span class="pcta-sub">Online, in-person, or by phone. Your call.</span>
    <span class="pcta-arrow" aria-hidden="true">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
    </span>
  </a>

  <!-- B: Secondary action — Current listings -->
  <a href="/listings" class="secondary-cta" data-cta="your_team_b_current_listings">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 01-2.83 0L2 12V2h10l8.59 8.59a2 2 0 010 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></svg>
    Current listings
    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
  </a>

  <!-- B: Off-market card as a single secondary item -->
  <section class="section" style="margin-top:0.5rem;">
    <a class="card" href="/buyers" data-cta="your_team_b_offmarket">
      <span class="card-icon">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="7.5" cy="15.5" r="4.5"/><path d="M10.7 12.3L21 2"/><path d="M16.5 6.5L19 9"/></svg>
      </span>
      <div class="card-body">
        <div class="card-title">Off-market & pre-market access</div>
        <p class="card-desc">See homes before they hit realestate.com.au.</p>
      </div>
      <svg class="card-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
    </a>
  </section>

  <!-- B: Compact team strip (moved BELOW actions). Two small cards with
       just headshot, name, call link, and More info button. Email removed
       to reduce visual noise; it's still on each /team/{slug} page. -->
  <p class="ts-label">Your Team</p>
  <div class="team-strip">
    {agents.map(a => (
      <div class="ts-card">
        <img src={a.photo} alt={a.name} class="ts-avatar" width="56" height="56" loading="lazy" />
        <div class="ts-body">
          <p class="ts-name">{a.name}</p>
          <p class="ts-row">
            <a href={`tel:${a.tel}`} class="ts-call" data-cta={`your_team_b_call_${a.ctaKey}`}>{a.phone}</a>
          </p>
          <a href={`/team/${a.slug}`} class="ts-more" data-cta={`your_team_b_more_${a.ctaKey}`}>
            More info
            <svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </a>
        </div>
      </div>
    ))}
  </div>

  <!-- B: Visual tile to recent listings. Replaces the article cards section
       so the page leans visual rather than text-heavy. -->
  <a href="/listings" class="visual-tile" data-cta="your_team_b_listings_tile">
    <img
      src="/_vercel/image?url=https%3A%2F%2Fcdn6.ep.dynamics.net%2Fs3%2Frw-propertyimages%2Ff0e3-H3512351-202426677__1779329460-52623-AMY9140.jpg&w=640&q=70"
      srcset="/_vercel/image?url=https%3A%2F%2Fcdn6.ep.dynamics.net%2Fs3%2Frw-propertyimages%2Ff0e3-H3512351-202426677__1779329460-52623-AMY9140.jpg&w=480&q=70 480w, /_vercel/image?url=https%3A%2F%2Fcdn6.ep.dynamics.net%2Fs3%2Frw-propertyimages%2Ff0e3-H3512351-202426677__1779329460-52623-AMY9140.jpg&w=960&q=70 960w"
      sizes="(max-width: 640px) 100vw, 480px"
      alt="Inner east Brisbane property"
      loading="lazy"
    />
    <div class="vt-overlay">
      <p class="vt-eyebrow">View now</p>
      <p class="vt-title">See all current listings</p>
      <p class="vt-arrow">&rarr;</p>
    </div>
  </a>

  <div class="contact-row">
    <a href="/contact" data-cta="your_team_b_contact">Contact</a>
    <a href="/tools" data-cta="your_team_b_all_tools">All tools</a>
    <a href="/" data-cta="your_team_b_home">Home</a>
  </div>

  <p class="legal">
    Educational information only. Always seek independent advice.
  </p>

</div>

<!-- B: Sticky mobile call bar. Visible only on small screens. -->
<a href="tel:+61412523821" class="mobile-call-bar" data-cta="your_team_b_mobile_sticky_call">
  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.61 3.39 2 2 0 0 1 3.6 1.21h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.83a16 16 0 0 0 5.92 5.92l.91-.91a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 21.21 16z"/></svg>
  Call Daniel &middot; 0412 523 821
</a>

</LandingLayout>
