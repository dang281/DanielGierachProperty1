---
// Clean shareable URL for the magazine-mode map (Daniel, 2026-07-31):
// renders the exact same page as /tools/property-map; the Layout nav
// script treats this path as nav=off so the masthead version shows
// without the query string. Content lives ONLY in property-map.astro.
import PropertyMap from './property-map.astro';
---
<PropertyMap />
