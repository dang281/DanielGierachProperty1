export type TodayItem = {
  slug: string  // pipeline, leads, referrals, etc.
  monday_item_id: string
  name: string | null
  stage: string | null
  follow_up: string | null
  nvml: string | null
  phone: string | null
  email: string | null
}
